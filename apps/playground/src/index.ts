import { APP_NAME, type Currency } from "@budget-flow/shared";

const defaultCurrency: Currency = "RUB";

console.log(`Application: ${APP_NAME}`);
console.log(`Default currency: ${defaultCurrency}`);
